# lambda

Code for `bedrock-asset-processor`.

I need the log line `Image received: <filename>` when a file lands in the assets bucket — that is what the exam checks.

Terraform’s serverless module zips and deploys `handler.py`. I do not edit this from the laptop to “test AWS” — I apply the stack.

[docs/modules/serverless.md](../docs/modules/serverless.md)
