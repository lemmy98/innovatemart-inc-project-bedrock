Retail store umbrella (v{{ .Chart.AppVersion }}).

EKS:
  kubectl get ingress -n {{ .Release.Namespace }}

Do not helm-install this chart on EKS while Terraform enable_app_deploy=true.
YAML alternative: k8s/ (see k8s/README.md).
